// Legacy placeholder: React build now handles UI (see /extension/ui)
// This file intentionally left minimal so existing manifest reference isn't required.
// Can be safely deleted if no longer referenced anywhere.
